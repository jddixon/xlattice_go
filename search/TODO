xlattice_go/search/TODO

2013-08-18
    * TestTopBottomMap is failing at two points, apparently because
        of a bug in Peer.Equal(), probably because of fields added
        to Peer struct.  Fix it.

    * AddToPeerMap's handling of the case where the byteVal 
        matches the existing root.byteVal is wrong. 
